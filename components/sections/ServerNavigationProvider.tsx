import NavigationProvider from './NavigationProvider'

// Server component wrapper that ensures all data is loaded on the server
export default function ServerNavigationProvider() {
  return <NavigationProvider />
}
